/* JavaScript File */
( function( $ ) {
    $( '.bxslider' ) .bxSlider({
        auto: true,   /* "Hacer que el Slider avance automáticamente" */
        mode: 'fade', /* Cambia el modo de transición a FADE */
        pause: 6000   /* Tiempo de duración. Por defecto son 4000ms (4 segundos) */
    });
}) (jQuery);
